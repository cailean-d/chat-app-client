import { MessageListPipe } from './message-list.pipe';

describe('MessageListPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageListPipe();
    expect(pipe).toBeTruthy();
  });
});
