import { StopBubblingDirective } from './stop-bubbling.directive';

describe('StopBubblingDirective', () => {
  it('should create an instance', () => {
    const directive = new StopBubblingDirective();
    expect(directive).toBeTruthy();
  });
});
